# Инструкция по деплою на Vercel

## Подготовка проекта

### Шаг 1: Убедитесь, что проект готов

Проект уже подготовлен к деплою:
- ✅ Создан `vercel.json` для конфигурации
- ✅ Создан `api/index.ts` для serverless functions
- ✅ Настроены переменные окружения
- ✅ Обновлен код для работы с Vercel URL

## Команды для отправки на GitHub

### Если у вас еще нет Git репозитория:

```bash
# Инициализация Git (если еще не сделано)
git init

# Добавление всех файлов
git add .

# Создание первого коммита
git commit -m "Initial commit: PulseTok TikTok OAuth integration"

# Создайте репозиторий на GitHub (https://github.com/new)
# Затем добавьте remote (замените YOUR_USERNAME и YOUR_REPO на свои значения)
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git

# Отправка на GitHub
git branch -M main
git push -u origin main
```

### Если репозиторий уже существует:

```bash
# Добавление всех изменений
git add .

# Создание коммита
git commit -m "Prepare project for Vercel deployment"

# Отправка на GitHub
git push
```

## Деплой на Vercel

### Шаг 1: Войти в Vercel

1. Перейдите на [https://vercel.com](https://vercel.com)
2. Нажмите "Sign Up" или "Log In"
3. Войдите через GitHub (рекомендуется)

### Шаг 2: Импортировать проект

1. На главной странице Vercel нажмите **"Add New..."** → **"Project"**
2. Выберите ваш GitHub репозиторий `pulsetok`
3. Нажмите **"Import"**

### Шаг 3: Настройка проекта в Vercel

#### Project Name:
```
pulsetok
```
(или любое другое имя)

#### Framework Preset:
```
Vite
```
(выберите из списка)

#### Root Directory:
```
./
```
(оставьте по умолчанию)

#### Build Command:
```
npm run build
```
(уже заполнено автоматически)

#### Output Directory:
```
dist
```
(уже заполнено автоматически)

#### Install Command:
```
npm install
```
(уже заполнено автоматически)

### Шаг 4: Настройка переменных окружения

**ВАЖНО:** Нажмите на **"Environment Variables"** перед деплоем!

Добавьте следующие переменные:

#### 1. TIKTOK_CLIENT_KEY
```
Key: TIKTOK_CLIENT_KEY
Value: aw3kxewbgdb39tz9
Environment: Production, Preview, Development (выберите все три)
```

#### 2. TIKTOK_CLIENT_SECRET
```
Key: TIKTOK_CLIENT_SECRET
Value: oefvbMlkQ3kKiqhpf41ICGQjlvzRBvD5
Environment: Production, Preview, Development (выберите все три)
```

#### 3. TIKTOK_REDIRECT_URI
```
Key: TIKTOK_REDIRECT_URI
Value: https://YOUR_VERCEL_DOMAIN.vercel.app/auth/callback
Environment: Production, Preview, Development
```
**Примечание:** Замените `YOUR_VERCEL_DOMAIN` на реальный домен, который Vercel присвоит вашему проекту (например: `pulsetok-abc123.vercel.app`). После первого деплоя вы получите URL, и нужно будет обновить это значение.

**Альтернатива:** Оставьте это поле пустым, и приложение автоматически использует VERCEL_URL.

#### 4. NODE_ENV
```
Key: NODE_ENV
Value: production
Environment: Production
```

#### 5. VERCEL_URL (опционально)
```
Key: VERCEL_URL
Value: (оставляем пустым, Vercel заполнит автоматически)
Environment: Production, Preview, Development
```

### Шаг 5: Деплой

1. После настройки всех переменных окружения нажмите **"Deploy"**
2. Дождитесь завершения сборки (обычно 2-5 минут)
3. После успешного деплоя вы получите URL вида: `https://pulsetok-abc123.vercel.app`

### Шаг 6: Обновление Redirect URI в TikTok

1. После деплоя получите ваш Vercel URL (например: `https://pulsetok-abc123.vercel.app`)
2. Перейдите в [TikTok Developer Portal](https://developers.tiktok.com/)
3. Откройте ваше приложение
4. Перейдите в "Login Kit" → "Web"
5. Добавьте новый Redirect URI:
   ```
   https://pulsetok-abc123.vercel.app/auth/callback
   ```
   (замените на ваш реальный Vercel URL)
6. Обновите переменную `TIKTOK_REDIRECT_URI` в Vercel на новый URL
7. Выполните новый деплой (Vercel сделает это автоматически при следующем push в GitHub)

## Обновление проекта после изменений

После внесения изменений в код:

```bash
# Добавить изменения
git add .

# Создать коммит
git commit -m "Описание изменений"

# Отправить на GitHub
git push
```

Vercel автоматически задеплоит новые изменения!

## Настройка кастомного домена (опционально)

1. В Vercel перейдите в настройки проекта → "Domains"
2. Добавьте ваш домен (например: `pulsetok.com`)
3. Настройте DNS записи согласно инструкциям Vercel
4. Обновите `TIKTOK_REDIRECT_URI` в Vercel на новый домен
5. Обновите Redirect URI в TikTok Developer Portal

## Troubleshooting

### Ошибка: "Module not found"
- Убедитесь, что все зависимости в `package.json`
- Проверьте, что `npm install` выполняется успешно

### Ошибка: "CORS error"
- Проверьте настройки CORS в `api/index.ts`
- Убедитесь, что `allowedOrigins` включает ваш Vercel URL

### Ошибка: "Invalid redirect URI"
- Убедитесь, что `TIKTOK_REDIRECT_URI` в Vercel точно совпадает с URL в TikTok Developer Portal
- Проверьте, что используется HTTPS (не HTTP)

### Ошибка: "Environment variable not found"
- Убедитесь, что все переменные окружения добавлены в Vercel
- Проверьте, что переменные доступны для нужного окружения (Production/Preview/Development)

## Полезные ссылки

- [Vercel Documentation](https://vercel.com/docs)
- [Vercel Environment Variables](https://vercel.com/docs/concepts/projects/environment-variables)
- [TikTok Developer Portal](https://developers.tiktok.com/)

## Чеклист перед деплоем

- [ ] Код отправлен на GitHub
- [ ] Проект импортирован в Vercel
- [ ] Все переменные окружения добавлены в Vercel
- [ ] Redirect URI обновлен в TikTok Developer Portal
- [ ] Проект успешно задеплоен
- [ ] Тестирование авторизации работает

Удачи с деплоем! 🚀

